name: Mocking NICS workflow

on:
  push:
    branches:
      - main

jobs:
  foo:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: foo
        uses: nvfp/demo-testing@3.0.25
        with:
          load: main
          dock: mocking-nics-workflow
          git-name: myname
          git-email: your@email
          gh-repo-name: demo
          container: test-dir