name: Mocking NICS workflow

on:
  push:
    branches:
      - main

jobs:
  foo:
    runs-on: ubuntu-latest
    steps:
      - name: foo
        uses: nvfp/demo-testing@3.0.23
        with:
          load: main
          dock: mocking-nics-workflow
          git-name: myname
          git-email: your@email
          gh-repo-name: demo
          container: test-dir