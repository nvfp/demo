name: Mocking NICS workflow

on:
  push:
    branches:
      - main

env:
  git-name: Foo bar
  git-email: your@email

jobs:
  foo:
    runs-on: ubuntu-latest
    steps:
      - name: Foo
        uses: nvfp/demo-testing@3.0.9
        with:
          foo: 1
          bar: 3