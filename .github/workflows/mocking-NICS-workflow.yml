name: Mocking NICS workflow

on:
  push:
    branches:
      - main

env:
  git_name: Foo bar
  git_email: your@email

jobs:
  foo:
    runs-on: ubuntu-latest
    steps:
      - name: foo
        uses: nvfp/demo-testing@3.0.19
        with:
          load: main
          dock: mocking-nics-workflow